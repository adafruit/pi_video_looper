# Sealed build defaults (Pi Zero-class, model-making)
# Copy bits you want into your live config.
[video_looper]
read_only = true
alive_file = /tmp/pi_model_looper.alive
alive_interval_sec = 10
preflight_media = true
